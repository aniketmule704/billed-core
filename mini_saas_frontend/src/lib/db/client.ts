// Stub — real implementation is mocked in smoke tests
export const query = {
  default: async (...args: any[]) => { throw new Error('Use vi.mock in tests') },
}

export const queryOne = {
  default: async (...args: any[]) => { throw new Error('Use vi.mock in tests') },
}
