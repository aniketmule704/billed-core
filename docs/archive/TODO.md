# BillZo Project Roadmap & TODO

## Status Overview
- **Phase A (First Recovered Rupee)**: 70% Complete
- **Phase B (Validate)**: 30% Complete
- **Phase C (Consolidation)**: 10% Complete

---

## 🚀 Phase A: First Recovered Rupee (CRITICAL)

### 1. Worker & Infrastructure
- [ ] **Worker Stability**: Verify `worker/index.ts` runs correctly on Fly.io/Render.
- [ ] **Supabase Consolidation**: Run `migration 029` and ensure all tables exist in Supabase.
- [ ] **Health Monitoring**: Verify `/health` endpoint in worker reports correct queue counts.
- [ ] **Logging**: Ensure `logWorkerEvent` and `logWorkerError` are shipping logs to a central location.

### 2. WhatsApp Connectivity
- [ ] **Baileys Pairing**: Test the full pairing flow: Frontend QR display → Scan → Redis Session → Worker Persistence.
- [ ] **Health Probes**: Ensure `probeChannelHealth` correctly updates `messaging_channels` table.
- [ ] **Reputation System**: Verify daily computation of `computeReputation`.

### 3. Recovery Flow
- [ ] **Backfill Recovery Cases**: Run backfill script to create cases from existing invoices in Neon/Supabase.
- [ ] **Reminder Queue**: Verify `enqueueOverdueReminders` correctly picks up overdue invoices.
- [ ] **WhatsApp Send**: Send one real reminder to a test merchant/customer.
- [ ] **Payment Reconciliation**: Verify that a payment event correctly resolves a `RecoveryCase`.

---

## 🛠️ Phase B: Validate & Refine

### 1. Frontend Enhancements
- [ ] **Queue UI**: Show real collectible amounts and situation narratives in the dashboard.
- [ ] **Situation Cards**: Implement the "Max 7 Situations" logic in the UI.
- [ ] **Manual Actions**: Allow merchants to manually snooze or mark cases as disputed.

### 2. Cognition & Intelligence
- [ ] **Situation Synthesis**: Refine the narratives generated by `worker/src/lib/cognition`.
- [ ] **Attention Scoring**: Tune the `computeAttentionScore` logic based on real data.

---

## 🏗️ Phase C: Database Consolidation & Scale

### 1. Architecture Refinement
- [ ] **Remove Neon Dependency**: Migrate all remaining business data from Neon to Supabase.
- [ ] **Authority Dual-Write**: Enable dual-write mode in `AuthorityGateway` for phased migration.
- [ ] **Mutation Gate**: Move from `shadow` mode to `enforce` mode once confident.

### 2. Advanced Features
- [ ] **Gupshup Integration**: Add paid fallback for WhatsApp Business API.
- [ ] **Razorpay Deep Sync**: Automate payment matching via Razorpay webhooks.

---

## 🧪 Testing & CI/CD
- [ ] **End-to-End Tests**: Implement Cypress/Playwright tests for the critical "Pairing -> Reminder -> Payment" path.
- [ ] **Policy Tests**: Add comprehensive tests for `AuthorityGateway` policies.
- [ ] **Type Safety**: Fix remaining type errors in `packages/shared`.

---

## 📝 Inline TODOs (Action Items)
- [ ] Fix FK types in `RecoveryCase` (Migration 028 verify).
- [ ] Implement `useAuth()` hook with JWT refresh support.
- [ ] Add Service Worker registration for PWA offline sync.
